import React from "react";

const Navbar = () => {
  return (
    <nav style={{ backgroundColor: "#1E40AF", color: "white", padding: "15px", fontSize: "20px" }}>
      Activity Tracker
    </nav>
  );
};

export default Navbar;
